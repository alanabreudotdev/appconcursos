import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:eusereiaprovado/app/modules/support/support_controller.dart';
import 'package:eusereiaprovado/app/modules/support/support_module.dart';

void main() {
  initModule(SupportModule());
  // SupportController support;
  //
  setUp(() {
    //     support = SupportModule.to.get<SupportController>();
  });

  group('SupportController Test', () {
    //   test("First Test", () {
    //     expect(support, isInstanceOf<SupportController>());
    //   });

    //   test("Set Value", () {
    //     expect(support.value, equals(0));
    //     support.increment();
    //     expect(support.value, equals(1));
    //   });
  });
}
