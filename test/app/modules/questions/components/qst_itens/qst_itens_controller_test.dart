import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:eusereiaprovado/app/modules/questions/components/qst_itens/qst_itens_controller.dart';
import 'package:eusereiaprovado/app/modules/questions/questions_module.dart';

void main() {
  initModule(QuestionsModule());
  // QstItensController qstitens;
  //
  setUp(() {
    //     qstitens = QuestionsModule.to.get<QstItensController>();
  });

  group('QstItensController Test', () {
    //   test("First Test", () {
    //     expect(qstitens, isInstanceOf<QstItensController>());
    //   });

    //   test("Set Value", () {
    //     expect(qstitens.value, equals(0));
    //     qstitens.increment();
    //     expect(qstitens.value, equals(1));
    //   });
  });
}
