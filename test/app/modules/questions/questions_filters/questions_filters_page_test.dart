import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:eusereiaprovado/app/modules/questions/questions_filters/questions_filters_page.dart';

main() {
  testWidgets('QuestionsFiltersPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(QuestionsFiltersPage(title: 'QuestionsFilters')));
    //  final titleFinder = find.text('QuestionsFilters');
    //  expect(titleFinder, findsOneWidget);
  });
}
